import React from 'react';
import AuthForm from '../Components/AuthForm';

const LoginPage = () => {
  // Render the AuthForm, by default it's a login form
  return <AuthForm />;
};

export default LoginPage;